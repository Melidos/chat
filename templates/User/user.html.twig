<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title></title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    </head>
    <body class="container-fluid">
        <div class="row">
            <div class="col-9 border-right"> <!-- Fenetre globale -->
                <!-- Chaque ligne de dialogue -->
                {% for message in messages %}
                    <div class="col-10 border" style='padding:auto; margin:auto;'>
                        <span class="text-left"><b>{{message.getUsername}}</b></span>
                        <div class="text-right" style="margin-top: -24px;">{{message.getDate.format('Y-m-d H:i:s')}}</div>
                        <div class="text-justify">{{message.getMessage}}</div>
                    </div>
                {% endfor %}
            </div>
            <div class="col-3">
                <strong>Users</strong>
                <div class="border-bottom" style="margin-bottom: 1em;"></div>
                <ul class="list-group list-group-flush">
                    {% for user in users %}
                        <li class="list-group-item">{{user}}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <div class="border-bottom" style="margin-top:10px;"></div>
        <form method="get" action="/message/add" class="fixed-bottom">
            <input type="text" name="pseudo" placeholder="Pseudo" style="width:98%; margin:0 1%;"/>
            <textarea name="message" placeholder="Votre message" style="width:98%; margin:0 1%;"></textarea>
            <input type="submit" value="Envoyer" style="display:block; margin-left:1%;"/>
        </form>
        <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
    </body>
</html>